# 更新日志 (Changelog)

> 本仓库为静态前端（GitHub Pages 部署），版本号仅作发布标记，不含编译产物。
> 数据由集群端定时生成并推送（`data/slurm.json`），页面每 5 分钟轮询检查更新。

## V5 · 2026-07-28

**主题：新增「平台公告」栏目，收尾本轮界面优化**

### 新增
- **「平台公告」栏目**：侧边栏首项新增四字栏目"平台公告"（喇叭图标 `ic-bullhorn`）。
  - **数据刷新策略**：由原右下角浮动通知（`#floatNotice`）整体迁移至此——日间 08:00–24:00 每 20 分钟刷新、夜间 00:00–08:00 每 2 小时刷新、页面每 5 分钟核查最新数据。
  - **功能简介**：说明页面用途（面向物理与光电工程学院师生，统一监控 HPC 集群运行状态），并以 3×3 特性卡片呈现 9 个核心模块（集群概览 / 分区资源 / 告警信息 / 磁盘空间 / QOS 约束 / 节点状态 / 任务列表 / 用户资源 / 在线终端）。
  - **排版**：采用纵向堆叠（上方"刷新策略"信息条 + 下方"功能九宫格"），消除首版左右两栏高度失衡；移动端九宫格降为 2 列。
  - 原右下角浮动通知 DOM / 样式 / 逻辑（`closeFloatNotice`、`restoreFloatNotice`、sessionStorage 相关）一并清理下线。
- 轮询间隔维持 **5 分钟**（`REFRESH_INTERVAL = 300000`）：页面检查频率独立于数据约 20 分钟的生成节奏，用于把"看到最新数据的延迟"压到 ≤5 分钟；数据未变时走条件请求 + 跳过重渲染，开销可忽略。

### 本轮并入的界面优化（已随前期提交上线，随 V5 一并归档）
- **SVG 图标体系**：内联 `<symbol>` 精灵（13 图标）替换侧栏/导航/额外项 emoji，跨平台一致、可随 `currentColor` 着色。
- **通知改造**：删除全视口 120s 漂移动画，改为静态卡片（本次被「平台公告」完全替代并移除）。
- **空态占位卡**：分区 / 磁盘 / 节点 / 任务 / 在线 / 用户数据为空时不再整节消失，显示占位卡（复用"暂无告警"模式）。
- **刷新失败提示**：`fetch` 失败时 `#updateTime` 显示"⚠ 刷新失败 · 上次成功"并加 `badge-warn`，不再静默。
- **设计系统**：引入半径 / 间距 token、`--bg-gradient` / `--shadow-lg` / `--footer-h`；JS 内联样式外提为语义类（`.kv-row` / `.sub-text` / `.cell-ellipsis` / `.mt-md`）。
- **无障碍**：新增 `prefers-reduced-motion` 守卫。

---

## V4 · 2026-07-28（前期提交，含于本轮发布基线）
- 前端代码审查 25 项问题修复：隐私脱敏（运行时不留真实用户名）、`esc()` 引号转义、`grid-1` 类与截断、搜索下拉定位、前端硬编码集群名移除。
- `INCAR.html` 参数外置为 `data/incar-data.json` 异步加载（页面 1MB→23KB）、CSP / meta / favicon 加固。
- `main.js` 渲染拆分为 10 个函数、数据未变跳过重渲染、`progressbar` 无障碍。
- 新增 `LICENSE`(MIT) / `.gitignore` / `README`（数据管道 + 浏览器基线）。
